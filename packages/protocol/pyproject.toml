[project]
name = "recombyn-protocol"
version = "0.1.3"
description = "Open Design / Intelligence protocol constants and schemas"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
  "pydantic>=2.9.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["recombyn_protocol*"]

# recombyn-plugin-sdk

Open helpers for `.recombyn-plugin` root `plugin.json` (ADR 0016).

Install / zip extract / HMAC verify stay in the API; this package validates the
manifest shape for pack authors and tooling.

/**
 * Public entry — generated FastAPI oRPC contract.
 * Regenerate: `npm run gen:contracts` (needs API on :8000 or PYTHONPATH to apps/api).
 */

export { contract as apiRouterContract } from '../generated/api/orpc.gen';

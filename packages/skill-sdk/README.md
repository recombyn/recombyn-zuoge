# recombyn-skill-sdk

Open helpers for Design Agent skill packs (`_meta.json` / `SKILL.md` layout).

Use this package to normalize pack meta (`skill_key`), parse `extends`, and sort pack versions — without depending on the full API skill_store runtime.

Shipped catalog lives under `skills/foundation` and `skills/domains` in the monorepo root.

# Good — task-first console

Why:
- Primary task is obvious (e.g. work queue)
- Largest region is the table / chart that serves that task
- KPIs absent, or one metric that changes the next click
- One primary action
- Placeholders (`—`) instead of invented numbers

# Dashboard visual review

Check that the largest region serves the primary task.
Fail if four (or three) equal KPI cards are the default composition
without user-supplied metrics that change the next action.
Fail if the board reads as a marketing landing or poster.

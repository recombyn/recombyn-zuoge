# Dashboard composition

Pick the main surface from the task (table, chart, canvas, inspector).
Chrome (sidebar / top bar) stays quiet and aligned.
Desktop ~1440×900. Do not crop a poster into a console.
Empty / loading / error states belong to the primary region.

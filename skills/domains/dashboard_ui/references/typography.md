# Dashboard type

Muted labels, bold values, one console type system.
Section titles quieter than the primary value or table header.
Action labels are verbs from user copy — not four identical “View”.

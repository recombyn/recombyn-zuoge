# AI section flow

```text
Hero → Live Demo → Input → AI → Output → Use Cases → Trust → CTA
```

The live demo (input → model → output) **is** the product.
Do not replace it with three equal “capability” cards.

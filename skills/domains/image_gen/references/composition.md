# Plate composition

Compose the bitmap for the board:

- One primary subject (`visual_hero`)
- Quiet bands for overlay type
- Crop that matches the surface archetype (center hero vs split landing)

Do not put a second screaming focal in the plate if the board already has a title fight.
